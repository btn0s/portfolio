import { Separator } from "@/components/ui/separator";
import WorkList from "@/components/work-list";

export const LAB_WORK_LIST_SLUGS = [
    "/lab/strella",
    // "/lab/pod",
    // "/lab/sketchbook",
    "/lab/game-dev-prototypes",
    "/lab/echelon",
    "/lab/head-2-head"
];

# My Lab  

**Where I explore ideas, build momentum, and follow curiosity into form.**
  
Some projects are rough, others more refined. But all of them share the same intent: to learn by building, and to make the invisible visible.

<div className="mb-12" />

<WorkList slugs={LAB_WORK_LIST_SLUGS} />